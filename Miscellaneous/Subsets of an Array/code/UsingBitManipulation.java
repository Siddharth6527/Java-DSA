import java.util.*;

public class UsingBitManipulation {
  // funciton to find the subsets of
  // the given array
  public static void findSubsets(int[] nums) {
    // get the length of the input array
    int n = nums.length;

    // loop through all possible subsets
    // using bit manipulation
    for (int i = 0; i < (1 << n); i++) {

    }

  }

}
